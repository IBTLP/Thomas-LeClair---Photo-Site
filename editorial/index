<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Editorial | Thomas LeClair Photography</title>
  <meta name="description" content="Editorial features by Thomas LeClair — long-form automotive stories and photo essays." />

  <style>
    :root{
      --bg:#0e0e0e;
      --panel:#141414;
      --text:#f2f2f2;
      --muted:rgba(242,242,242,.72);
      --muted2:rgba(242,242,242,.58);
      --line:rgba(255,255,255,.12);
      --shadow: 0 18px 60px rgba(0,0,0,.55);
      --radius:18px;
      --radius2:26px;
      --max: 1120px;
    }

    *{ box-sizing:border-box; }
    body{
      margin:0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background:var(--bg);
      color:var(--text);
      line-height:1.55;
    }
    a{ color:inherit; text-decoration:none; }
    a:hover{ opacity:.95; }

    /* Nav */
    .nav{
      position:sticky;
      top:0;
      z-index:50;
      background: rgba(14,14,14,.78);
      backdrop-filter: blur(10px);
      border-bottom:1px solid var(--line);
    }
    .nav-inner{
      max-width:var(--max);
      margin:0 auto;
      padding:14px 18px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:14px;
    }
    .brand{
      display:flex;
      align-items:center;
      gap:10px;
      font-weight:700;
      letter-spacing:.2px;
    }
    .brand-dot{
      width:10px;height:10px;border-radius:50%;
      background:#fff; opacity:.9;
      box-shadow: 0 0 0 4px rgba(255,255,255,.08);
    }
    .navlinks{
      display:flex;
      gap:14px;
      flex-wrap:wrap;
      justify-content:flex-end;
      font-size:14px;
      color:var(--muted);
    }
    .navlinks a{
      padding:8px 10px;
      border-radius:999px;
      border:1px solid transparent;
    }
    .navlinks a:hover{
      border-color: var(--line);
      background: rgba(255,255,255,.04);
      color:var(--text);
    }

    /* Layout */
    .wrap{ max-width:var(--max); margin:0 auto; padding: 22px 18px 70px; }
    h1{
      margin: 12px 0 6px;
      font-size: clamp(26px, 3.6vw, 40px);
      letter-spacing:-.02em;
      line-height:1.1;
    }
    .sub{
      margin:0 0 18px;
      color:var(--muted);
      max-width: 78ch;
      font-size: 15px;
    }

    /* Cards grid */
    .grid{
      display:grid;
      grid-template-columns: repeat(12, 1fr);
      gap: 14px;
      margin-top: 16px;
    }
    @media (max-width: 980px){
      .grid{ grid-template-columns: repeat(6, 1fr); }
    }
    @media (max-width: 640px){
      .grid{ grid-template-columns: repeat(1, 1fr); }
    }

    .card{
      grid-column: span 6;
      border: 1px solid var(--line);
      border-radius: var(--radius2);
      overflow:hidden;
      background: #0b0b0b;
      box-shadow: 0 18px 45px rgba(0,0,0,.35);
      display:flex;
      flex-direction:column;
      min-height: 360px;
    }
    @media (max-width: 640px){
      .card{ grid-column: span 1; min-height: 340px; }
    }

    .thumb{
      position:relative;
      aspect-ratio: 16/10;
      overflow:hidden;
      background: #0b0b0b;
    }
    .thumb img{
      width:100%;
      height:100%;
      object-fit:cover;
      display:block;
      transform: scale(1.01);
      transition: transform .28s ease;
      filter: contrast(1.02) saturate(1.02);
    }
    .card:hover .thumb img{ transform: scale(1.045); }

    .badge{
      position:absolute;
      left: 14px;
      top: 14px;
      padding: 7px 10px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,.18);
      background: rgba(0,0,0,.26);
      color: rgba(255,255,255,.82);
      font-size: 12px;
      letter-spacing: .12em;
      text-transform: uppercase;
    }

    .content{
      padding: 16px 16px 14px;
      display:flex;
      flex-direction:column;
      gap: 10px;
      flex:1;
    }
    .title{
      margin:0;
      font-size: 18px;
      letter-spacing:-.01em;
      line-height:1.2;
    }
    .desc{
      margin:0;
      color: rgba(255,255,255,.78);
      font-size: 14px;
      max-width: 70ch;
    }

    .meta{
      margin-top:auto;
      display:flex;
      flex-wrap:wrap;
      gap:8px;
      color: var(--muted2);
      font-size: 13px;
    }
    .pill{
      border:1px solid var(--line);
      background: rgba(255,255,255,.03);
      padding:8px 10px;
      border-radius:999px;
    }

    .actions{
      display:flex;
      gap:10px;
      flex-wrap:wrap;
      padding: 0 16px 16px;
    }
    .btn{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap:10px;
      padding:10px 12px;
      border-radius:999px;
      border:1px solid var(--line);
      background: rgba(255,255,255,.05);
      color:var(--text);
      font-size:14px;
      cursor:pointer;
    }
    .btn:hover{ background: rgba(255,255,255,.08); }
    .btn.primary{ background: rgba(255,255,255,.12); }

    footer{
      margin-top: 22px;
      color: var(--muted2);
      font-size: 13px;
      text-align:center;
    }
  </style>
</head>

<body>
  <header class="nav">
    <div class="nav-inner">
      <a class="brand" href="../index.html" aria-label="Home">
        <span class="brand-dot"></span>
        <span>Thomas LeClair Photography</span>
      </a>
      <nav class="navlinks" aria-label="Site">
        <a href="../index.html">Home</a>
        <a href="../gallery/">Gallery</a>
        <a href="./">Editorial</a>
        <a href="../prints.html">Prints</a>
        <a href="../contact.html">Contact</a>
      </nav>
    </div>
  </header>

  <main class="wrap">
    <h1>Editorial</h1>
    <p class="sub">
      Long-form stories and photo essays — builds, details, and the moments in-between.
      Each feature is designed to read clean on mobile and feel like a magazine spread.
    </p>

    <section class="grid">

      <!-- CARD: 56-Wagon -->
      <article class="card">
        <a class="thumb" href="./56-Wagon/" aria-label="Open 1956 Wagon Feature">
          <span class="badge">Feature</span>
          <img src="./56-Wagon/WagonHero.webp" alt="1956 wagon hero image" loading="lazy" decoding="async">
        </a>
        <div class="content">
          <h2 class="title">1956 Belair Wagon Feature</h2>
          <p class="desc">
            A classic silhouette with modern reliability — built to drive, finished to stop you in your tracks.
          </p>
          <div class="meta">
            <span class="pill">JRW Rods &amp; Customs</span>
            <span class="pill">Photo story + PDF</span>
          </div>
        </div>
        <div class="actions">
          <a class="btn primary" href="./56-Wagon/">Read</a>
          <a class="btn" href="./56-Wagon/56-wagon-feature.pdf" target="_blank" rel="noopener">PDF</a>
        </div>
      </article>

      <!-- CARD: C-10 Tiffany -->
      <article class="card">
        <a class="thumb" href="./C-10_Tiffany/" aria-label="Open C-10 Tiffany Feature">
          <span class="badge">Feature</span>
          <!-- If you don't have a hero image yet, this will still work if you drop one in later -->
          <img src="./C-10_Tiffany/Hero.webp" alt="C-10 Tiffany hero image" loading="lazy" decoding="async"
               onerror="this.style.display='none'; this.parentElement.style.background='linear-gradient(135deg, rgba(255,255,255,.08), rgba(255,255,255,.02))';">
        </a>
        <div class="content">
          <h2 class="title">C-10 Tiffany</h2>
          <p class="desc">
            A clean, modern build with a classic backbone — a story driven by details, stance, and intention.
          </p>
          <div class="meta">
            <span class="pill">Editorial</span>
            <span class="pill">In progress</span>
          </div>
        </div>
        <div class="actions">
          <a class="btn primary" href="./C-10_Tiffany/">Read</a>
          <!-- Optional: add a PDF later, remove this button if you don't want it yet -->
          <!-- <a class="btn" href="./C-10_Tiffany/c-10-tiffany-feature.pdf" target="_blank" rel="noopener">PDF</a> -->
        </div>
      </article>

    </section>

    <footer>
      © <span id="year"></span> Thomas LeClair Photography
    </footer>
  </main>

  <script>
    document.getElementById("year").textContent = new Date().getFullYear();
  </script>
</body>
</html>
